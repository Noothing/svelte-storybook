import NewInput from "./NewInput.svelte";

export default {
    title: 'NewInput',
    component: NewInput,
}

export const Default = {
    args: {
        value: 'Test',
        editable: true
    }
}